---
interface Props {
	href: string;
}

const { href } = Astro.props;
---

<a class="flex gap-x-2 items-center rounded-full py-2 px-4 border border-white/20 bg-white/5 hover:scale-110 hover:bg-white/10 transition" 
	href={href} target="_blank">
	<slot />
</a>
