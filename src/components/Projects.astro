---
const PROJECTS = [
  {
    title: 'Pokedex',
    description: 'Aplicación de Pokemon donde encontraras los pokemones',
    link: 'https://dg-pokedex.netlify.app/',
    repo: 'https://github.com/dagamdev/Pokedex',
    image: './pokedex.webp',
    tags: [
      'HTML',
      'CSS',
      'JavaScript'
    ]
  }
]
---

<ul>
  {PROJECTS.map(project => <article>
      <h3 class="text-2xl font-semibold text-blue-200 mb-2">{project.title}</h3>
      <p class="text-lg mb-4 text-pretty">{project.description}</p>
      <ul>
        {project.tags.map(tag => <li>{tag}</li>)}  
      </ul>
      <img 
        class="rounded-md shadow-xl shadow-white/10"
        src={project.image} 
        alt={`Screenshot de ${project.title}`}
      >
    </article>)}
</ul>