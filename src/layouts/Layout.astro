---
import '@fontsource-variable/onest'
import Header from '@/components/Header.astro'
import Footer from '@/components/Footer.astro'

interface Props {
	title: string
	description?: string
	inHome?: boolean
}

const { title, description, inHome = false } = Astro.props
---

<!doctype html>
<html lang="en">
	<head>
		<meta charset="UTF-8" />
		<meta name="description" content={description} />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<title>{title}</title>
	</head>
	<body class="text-gray-900 dark:text-white">
		<div class="absolute top-0 bottom-0 left-0 z-[-2] min-h-screen w-full bg-neutral-100 dark:bg-neutral-950 bg-[radial-gradient(ellipse_80%_80%_at_50%_-20%,rgba(120,119,198,0.3),rgba(255,255,255,0))]"></div>
		<Header inHome={inHome} />
		<slot />
		<Footer />
	</body>
</html>

<style is:global>
	:root {
		color-scheme: light dark;
	}

	html {
		font-family: 'Onest', sans-serif;
		scroll-behavior: smooth;
	}

	body {
		position: relative;
		min-height: 100vh;
		background-color: #050505;
	}

	@media (prefers-reduced-motion: reduce) {
		html {
			scroll-behavior: auto;
		}
	}

	.paragraph {
    display: -webkit-box;
    overflow: hidden;
    -webkit-box-orient: vertical;
    text-overflow: ellipsis;
  }

  .limit {
    -webkit-line-clamp: 5;
  }
</style>
